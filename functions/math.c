#include <stdio.h>
#include <math.h>

int main()
{
    printf("%.0f \n", sqrt(16));
    printf("%.0f \n", ceil(1.4));
    printf("%.0f \n", floor(1.4));
    printf("%.0f \n", pow(4, 3));

    return 0;
}